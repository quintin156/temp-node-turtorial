// Global Variables - NO WINDOW!!

// __dirname  = path to current working directory
// __filename = file name to
// require()  = function to use modules
// module     = info about current module (file or directory)
// process    = info about env(enviroment) where the program is running/being executed
// setInterval() = function to set the interval that takes a callback and time it runs with what time is specified
